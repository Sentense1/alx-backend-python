#!/usr/bin/env python3
""" Annotated function that concats. two strings """


def concat(str1: str, str2: str) -> str:
    """
    function concat that takes two
    strings and returns a concatenated string
    """
    return '{}{}'.format(str1, str2)
